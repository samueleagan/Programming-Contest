#include <iostream>
#include <string>

using namespace std;

// 2373
// Base 8: 2^8 + 2^3 + 2^7 + 3
// Base 10: 2^10 + 2^3 + 2^7 + 3


int main() {
    int input;


    while (input != 0) {
        cin >> input;
        string s = to_string(input);
        int value = stoi(s);
        int lastElement = s[s.size() - 1];





    }

    return 0;

}
